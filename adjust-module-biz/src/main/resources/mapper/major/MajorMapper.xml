<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hongguoyan.module.biz.dal.mysql.major.MajorMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="selectSuggestMajorCodes" resultType="java.lang.String">
        SELECT DISTINCT code
        FROM biz_major
        WHERE code LIKE CONCAT(#{keyword}, '%')
          AND year = #{year}
        ORDER BY code
        LIMIT #{limit}
    </select>

    <select id="selectSuggestMajorNames" resultType="java.lang.String">
        SELECT DISTINCT name
        FROM biz_major
        WHERE name LIKE CONCAT(#{keyword}, '%')
          AND year = #{year}
        ORDER BY name
        LIMIT #{limit}
    </select>

    <select id="selectLevel1List"
            resultType="com.hongguoyan.module.biz.controller.app.major.vo.AppMajorLevel1RespVO">
        SELECT code, name
        FROM biz_major
        WHERE level = 1
          AND year = #{year}
        ORDER BY code
    </select>

</mapper>